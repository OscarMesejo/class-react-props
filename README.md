# React props

In this project we are going to learn about props in React.

We create a few components and pass the json data to them as props.
We are going to create another component inside a component and pass the props again to it.

## Fork the repo

Fork the repo and clone it to your local machine.
Use `npm install` to install the dependencies.
Use `npm run dev` to run the project locally.